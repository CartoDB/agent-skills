description = "Write spatial SQL on Oracle. CARTO's Analytics Toolbox is NOT available on Oracle today — Oracle is supported only as a connection target for Builder / Workflows / Maps. This skill documents the gap and steers the agent to native Oracle Spatial (SDO_GEOMETRY, SDO_RELATE, SDO_GEOM.*) instead of failing-silently with carto.* calls (carto-skills)"

prompt = """
Apply the `carto-spatial-sql-oracle` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-spatial-sql-oracle/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-spatial-sql-oracle/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
