description = "Guides the user through building composite-score workflows when they ask about composite scores, indexes, multi-variable rankings, market potential, resilience indexes, risk indexes, weighted scores, PCA, or supervised/unsupervised scoring (carto-skills)"

prompt = """
Apply the `carto-pattern-composite-scoring` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-pattern-composite-scoring/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-pattern-composite-scoring/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
