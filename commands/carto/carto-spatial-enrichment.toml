description = "Guides the user through spatial enrichment workflows — triggered by requests to enrich, add demographics, estimate population around locations, compute spatial features, sociodemographic analysis, "what's around" queries, buffer/isochrone + join patterns, or trade area enrichment (carto-skills)"

prompt = """
Apply the `carto-spatial-enrichment` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-spatial-enrichment/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-spatial-enrichment/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
