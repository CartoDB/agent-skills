description = "Preview an existing saved CARTO Builder map inline in the chat via the CARTO MCP server's load_builder_map tool. Resolves URL / ID / name (search via list_maps), renders a lightweight read-only preview (layers, basemap, viewport, popups, legend; widgets / SQL parameters / map description NOT included). Triggers on 'show me the X map', 'open the Y map', and post-CLI-creation inline previews (carto-skills)"

prompt = """
Apply the `carto-preview-builder-map` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-preview-builder-map/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-preview-builder-map/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
