description = "Analyse CARTO Self-Hosted support bundles — triage installation failures, degraded status, and deployment gaps from KOTS-generated bundle directories (carto-skills)"

prompt = """
Apply the `carto-sh-support-bundle` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-sh-support-bundle/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-sh-support-bundle/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
