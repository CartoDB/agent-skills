description = "Write spatial SQL on Amazon Redshift using CARTO's Analytics Toolbox. The headline gotcha: no standalone H3 module on Redshift — use quadbin for binning. Covers AT modules that DO ship, Redshift's 2D-only GEOMETRY history, SRID-0 default, and the sort-key / dist-style pattern for spatial joins (carto-skills)"

prompt = """
Apply the `carto-spatial-sql-redshift` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-spatial-sql-redshift/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-spatial-sql-redshift/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
