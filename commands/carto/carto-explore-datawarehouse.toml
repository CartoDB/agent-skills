description = "Discover what's in the connected warehouse: schemas, tables, columns, named sources (carto-skills)"

prompt = """
Apply the `carto-explore-datawarehouse` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-explore-datawarehouse/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-explore-datawarehouse/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
