description = "Enumerate ArcGIS Portal / AGOL items, classify by type, and write MIGRATION_MANIFEST.md so the agent and the user know what to migrate and how. Use this first when starting a Portal-to-CARTO migration (carto-skills)"

prompt = """
Apply the `carto-arcgis-discover` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-arcgis-discover/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-arcgis-discover/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
