description = "Administer the CARTO org — users, roles, quotas, activity audit, and bulk resource operations (carto-skills)"

prompt = """
Apply the `carto-manage-platform` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-manage-platform/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-manage-platform/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
