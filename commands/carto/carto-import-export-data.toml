description = "Import geospatial files into the data warehouse via CARTO, export results back out, and prepare tilesets for fast map rendering (carto-skills)"

prompt = """
Apply the `carto-import-export-data` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-import-export-data/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-import-export-data/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
