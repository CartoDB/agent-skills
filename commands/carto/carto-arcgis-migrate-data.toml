description = "Migrate Hosted Feature Layers and Hosted Tables from an ArcGIS Portal/AGOL to CARTO data warehouse tables. Walks the manifest's Datasets section, extracts each pending entry as GeoParquet, imports via `carto import`, and updates per-item state. Default mode is batch. Use after carto-arcgis-discover (carto-skills)"

prompt = """
Apply the `carto-arcgis-migrate-data` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-arcgis-migrate-data/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-arcgis-migrate-data/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
