description = "Builds geocoding workflows in CARTO that convert street addresses or place names into geographic coordinates. Triggers when the user has tabular data with addresses but no geometry column and needs to create one (carto-skills)"

prompt = """
Apply the `carto-pattern-geocoding` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-pattern-geocoding/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-pattern-geocoding/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
