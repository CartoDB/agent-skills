description = "Migrate ArcGIS Web Maps — and simple ArcGIS Dashboards / Web Experiences / Web Mapping Applications flagged with `Routing decision: builder` by discover — into CARTO Builder maps. Translates renderers, popups, supported Arcade subset, and basemap into a keplerMapConfig JSON; for simple-app entries, enables Builder map controls and emits analytical widgets; validates and creates via `carto maps`; tags every migrated map `From ArcGIS`. Default mode is batch. Always private. Use after carto-arcgis-migrate-data (carto-skills)"

prompt = """
Apply the `carto-arcgis-migrate-maps` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-arcgis-migrate-maps/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-arcgis-migrate-maps/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
