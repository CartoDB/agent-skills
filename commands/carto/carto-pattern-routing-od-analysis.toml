description = "Builds routing and origin-destination analysis workflows in CARTO. Triggers on routing, travel time, OD matrix, isoline, isochrone, catchment area, drive-time polygon, accessibility analysis, commute patterns, OD flow, or shortest-path computation (carto-skills)"

prompt = """
Apply the `carto-pattern-routing-od-analysis` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-pattern-routing-od-analysis/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-pattern-routing-od-analysis/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
