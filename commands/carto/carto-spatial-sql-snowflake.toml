description = "Write spatial SQL on Snowflake using CARTO's Analytics Toolbox. Covers the AT modules shipped (and missing — no cpg/geohash/telco), Snowflake's GEOGRAPHY vs GEOMETRY split, Search Optimization vs clustering-key tradeoffs, and the Native App marketplace install path (carto-skills)"

prompt = """
Apply the `carto-spatial-sql-snowflake` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-spatial-sql-snowflake/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-spatial-sql-snowflake/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
