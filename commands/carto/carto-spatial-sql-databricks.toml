description = "Write spatial SQL on Databricks (Lakehouse, Photon). CARTO's AT here is narrow — enrichment + LDS + quadbin + statistics only — and delegates H3 and ST_* primitives to Databricks-native spatial SQL. Covers the split, string-form vs BIGINT H3, and Z-order / liquid-clustering patterns (carto-skills)"

prompt = """
Apply the `carto-spatial-sql-databricks` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-spatial-sql-databricks/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-spatial-sql-databricks/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
