description = "Write spatial SQL on PostgreSQL / PostGIS. CARTO's AT here is thin (h3, quadbin, tiler only) — PostGIS native covers everything else, with no AT-based enrichment / statistics / LDS available. Covers install gotchas (plv8 for H3), lowercase identifier folding, and GiST / SP-GiST / BRIN index choice (carto-skills)"

prompt = """
Apply the `carto-spatial-sql-postgres` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-spatial-sql-postgres/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-spatial-sql-postgres/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
