description = "Computes new rasters from existing ones in CARTO with raster algebra (pixel-by-pixel expressions over one or more RaQuet rasters). Triggers when the user wants to subtract, add, compare, combine, threshold or mask rasters, compute band math or spectral indices (NDVI, NDWI, NBR...), change detection or before/after deltas (e.g. signal coverage gained by a new site), map algebra, raster calculator, or "operations between two raster sources and their bands" — in the Analytics Toolbox (SQL) or in Workflows (carto-skills)"

prompt = """
Apply the `carto-raster-algebra` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-raster-algebra/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-raster-algebra/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
