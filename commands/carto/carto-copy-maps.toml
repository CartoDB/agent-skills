description = "Copy maps across CARTO organizations or profiles, with connection mapping and AI-agent reference handling (carto-skills)"

prompt = """
Apply the `carto-copy-maps` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-copy-maps/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-copy-maps/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
