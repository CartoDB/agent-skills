description = "Builds site selection and cannibalization analysis workflows in CARTO. Triggers on site selection, cannibalization, new store location, optimal location, twin areas, look-alike areas, or revenue impact of a new store (carto-skills)"

prompt = """
Apply the `carto-pattern-site-selection` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-pattern-site-selection/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-pattern-site-selection/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
