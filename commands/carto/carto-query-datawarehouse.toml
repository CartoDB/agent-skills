description = "Run SQL against any CARTO-connected warehouse: `sql query` vs `sql job` execution model, caching, and a dispatcher table that routes to the per-engine spatial-SQL skill (BigQuery, Snowflake, Databricks, Postgres, Redshift, Oracle) for dialect-specific Analytics Toolbox content (carto-skills)"

prompt = """
Apply the `carto-query-datawarehouse` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-query-datawarehouse/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-query-datawarehouse/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
