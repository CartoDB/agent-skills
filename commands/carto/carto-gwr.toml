description = "Builds Geographically Weighted Regression (GWR) workflows in CARTO. Triggers when the user mentions GWR, geographically weighted regression, spatially varying relationships, local regression, local coefficients, spatial non-stationarity, or wants to model how predictor importance shifts across geography (carto-skills)"

prompt = """
Apply the `carto-gwr` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-gwr/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-gwr/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
