description = "Build, schedule, and operate analytics DAGs in CARTO Workflows — the no-code/low-code orchestration layer over the data warehouse (carto-skills)"

prompt = """
Apply the `carto-create-analytics-workflow` skill from the carto-skills catalog to the user's request.

Required behavior:
- Read the skill's main guidance at `skills/carto-create-analytics-workflow/SKILL.md` first.
- Follow the skill's reference files (under `skills/carto-create-analytics-workflow/references/`) for depth on specific topics.
- Stay within the skill's stated scope; if the request belongs to a different skill, say which one and stop.
- For machine-parseable CLI output, always pass `--json`.
"""
